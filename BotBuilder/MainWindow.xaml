<Window x:Class="BotBuilder.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:BotBuilder"
        xmlns:targets="clr-namespace:BotBuilder.Core.Targets;assembly=BotBuilder.Core"
        xmlns:core="clr-namespace:BotBuilder.Core;assembly=BotBuilder.Core"
        Title="{Binding WindowTitle}" Height="720" Width="1100"
        KeyDown="Window_KeyDown" Background="{DynamicResource WindowBackgroundBrush}">
    <Window.Resources>
        <local:CategoryColorToBrushConverter x:Key="CategoryBrush" />
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <local:PathToImageConverter x:Key="PathToImage" />

        <DataTemplate x:Key="FieldString">
            <StackPanel Margin="0,4">
                <TextBlock Text="{Binding Label}" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" />
                <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
        </DataTemplate>
        <DataTemplate x:Key="FieldMultiline">
            <StackPanel Margin="0,4">
                <TextBlock Text="{Binding Label}" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" />
                <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True" TextWrapping="Wrap" MinHeight="48" VerticalScrollBarVisibility="Auto" />
            </StackPanel>
        </DataTemplate>
        <DataTemplate x:Key="FieldNumber">
            <StackPanel Margin="0,4">
                <TextBlock Text="{Binding Label}" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" />
                <!-- Commit on focus-loss, NOT per keystroke: a numeric field also accepts a ${var}
                     expression, and per-keystroke coercion would turn each partial keystroke into 0
                     before the whole ${...} token is typed. On blur the full value is coerced once. -->
                <TextBox Text="{Binding Value, UpdateSourceTrigger=LostFocus}" />
            </StackPanel>
        </DataTemplate>
        <DataTemplate x:Key="FieldBoolean">
            <CheckBox Content="{Binding Label}" IsChecked="{Binding Value}" Margin="0,6" />
        </DataTemplate>
        <DataTemplate x:Key="FieldEnum">
            <StackPanel Margin="0,4">
                <TextBlock Text="{Binding Label}" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" />
                <ComboBox ItemsSource="{Binding Options}" SelectedItem="{Binding Value}" />
            </StackPanel>
        </DataTemplate>
        <DataTemplate x:Key="FieldFilePath">
            <StackPanel Margin="0,4">
                <TextBlock Text="{Binding Label}" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" />
                <DockPanel>
                    <Button DockPanel.Dock="Right" Content="Browse..." Margin="4,0,0,0" Padding="6,0"
                            Click="BrowseField_Click" />
                    <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" />
                </DockPanel>
            </StackPanel>
        </DataTemplate>
        <DataTemplate x:Key="FieldImagePath">
            <StackPanel Margin="0,4">
                <TextBlock Text="{Binding Label}" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" />
                <DockPanel>
                    <Button DockPanel.Dock="Right" Content="Browse..." Margin="4,0,0,0" Padding="6,0"
                            Click="BrowseField_Click" />
                    <Button DockPanel.Dock="Right" Content="Capture" Margin="4,0,0,0" Padding="6,0"
                            Click="CaptureField_Click" />
                    <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" />
                </DockPanel>
                <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Margin="0,4,0,0" Background="{DynamicResource SurfaceBackgroundBrush}"
                        HorizontalAlignment="Left">
                    <Image Source="{Binding Value, Converter={StaticResource PathToImage}}"
                           MaxHeight="120" MaxWidth="220" Stretch="Uniform" Margin="2" />
                </Border>
            </StackPanel>
        </DataTemplate>

        <local:ConfigFieldTemplateSelector x:Key="FieldSelector"
            StringTemplate="{StaticResource FieldString}"
            MultilineTemplate="{StaticResource FieldMultiline}"
            NumberTemplate="{StaticResource FieldNumber}"
            BooleanTemplate="{StaticResource FieldBoolean}"
            EnumTemplate="{StaticResource FieldEnum}"
            FilePathTemplate="{StaticResource FieldFilePath}"
            ImagePathTemplate="{StaticResource FieldImagePath}" />
    </Window.Resources>
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="_New" Click="New_Click" />
                <MenuItem Header="_Open..." Click="Open_Click" />
                <MenuItem Header="_Save..." Click="Save_Click" />
            </MenuItem>
            <MenuItem Header="_Edit">
                <MenuItem Header="_Undo" Click="Undo_Click" InputGestureText="Ctrl+Z" />
                <MenuItem Header="_Redo" Click="Redo_Click" InputGestureText="Ctrl+Y" />
                <Separator />
                <MenuItem Header="_Delete" Click="Delete_Click" InputGestureText="Del" />
                <Separator />
                <MenuItem Header="_Tidy Up" Click="TidyUp_Click" InputGestureText="Ctrl+L" />
            </MenuItem>
            <MenuItem Header="_Run">
                <MenuItem Header="_Test Run..." Click="TestRun_Click" InputGestureText="F5" />
            </MenuItem>
            <MenuItem Header="_View">
                <MenuItem Header="_Theme">
                    <MenuItem x:Name="ThemeSystemItem" Header="_System" IsCheckable="True" Click="ThemeSystem_Click" />
                    <MenuItem x:Name="ThemeLightItem" Header="_Light" IsCheckable="True" Click="ThemeLight_Click" />
                    <MenuItem x:Name="ThemeDarkItem" Header="_Dark" IsCheckable="True" Click="ThemeDark_Click" />
                    <MenuItem x:Name="ThemeHighContrastItem" Header="_High Contrast" IsCheckable="True" Click="ThemeHighContrast_Click" />
                </MenuItem>
                <Separator />
                <MenuItem Header="_Fit to Window" Click="FitToWindow_Click" InputGestureText="Ctrl+Shift+0" />
                <MenuItem Header="Reset _Zoom" Click="ResetZoom_Click" InputGestureText="Ctrl+0" />
            </MenuItem>
        </Menu>

        <Border DockPanel.Dock="Top" Background="{DynamicResource PanelBackgroundBrush}" Padding="6" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Targets:" VerticalAlignment="Center" Margin="0,0,8,0" FontWeight="Bold" />
                <ItemsControl ItemsSource="{Binding TargetBar.Targets}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate><StackPanel Orientation="Horizontal" /></ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{DynamicResource SurfaceBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4"
                                    Padding="4,2" Margin="0,0,6,0">
                                <StackPanel Orientation="Horizontal">
                                    <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Width="90"
                                             BorderThickness="0" VerticalAlignment="Center" />
                                    <ComboBox ItemsSource="{x:Static targets:TargetViewModel.AllTypes}"
                                              SelectedItem="{Binding Type}" Margin="4,0" VerticalAlignment="Center" />
                                    <TextBox Text="{Binding Selector, UpdateSourceTrigger=PropertyChanged}" Width="130"
                                             VerticalAlignment="Center" ToolTip="selector" />
                                    <Button Content="Pick…" Click="PickSelector_Click" Margin="4,0,0,0" Padding="4,0"
                                            ToolTip="Pick from open windows / devices / browsers" />
                                    <Button Content="x" Click="RemoveTarget_Click" Margin="4,0,0,0" Padding="4,0"
                                            ToolTip="Remove target" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <Button Content="+ Add Target" Click="AddTarget_Click" Padding="6,2" />
            </StackPanel>
        </Border>

        <local:LogPanelView x:Name="LogPanel" DockPanel.Dock="Bottom" Height="180" Visibility="Collapsed" />
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBar.ItemsPanel>
                <ItemsPanelTemplate><DockPanel /></ItemsPanelTemplate>
            </StatusBar.ItemsPanel>
            <StatusBarItem><TextBlock Text="{Binding BotName}" /></StatusBarItem>
            <StatusBarItem><TextBlock Text="{Binding Nodes.Count, StringFormat='Nodes: {0}'}" /></StatusBarItem>
            <StatusBarItem DockPanel.Dock="Right">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding Viewport.ZoomPercent, StringFormat='Zoom: {0}%'}"
                               VerticalAlignment="Center" Margin="0,0,8,0" />
                    <Button Content="Fit" Click="FitToWindow_Click" Padding="6,0" Margin="0,0,4,0"
                            ToolTip="Frame all nodes (Ctrl+Shift+0)" />
                    <Button Content="100%" Click="ResetZoom_Click" Padding="6,0"
                            ToolTip="Reset zoom to 100% (Ctrl+0)" />
                </StackPanel>
            </StatusBarItem>
            <StatusBarItem><TextBlock x:Name="RunStatusText" Foreground="{DynamicResource SecondaryTextBrush}" /></StatusBarItem>
        </StatusBar>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="240" />
            </Grid.ColumnDefinitions>

            <DockPanel Grid.Column="0" Background="{DynamicResource PanelBackgroundBrush}">
                <TextBox DockPanel.Dock="Top" Margin="6"
                         Text="{Binding Palette.SearchText, UpdateSourceTrigger=PropertyChanged}" />
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Palette.Categories}" Margin="6,0">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,4">
                                <TextBlock Text="{Binding Name}" FontWeight="Bold" Margin="0,2"
                                           ToolTip="{Binding DisabledReason}">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsAvailable}" Value="False">
                                                    <Setter Property="Foreground" Value="{DynamicResource DisabledTextBrush}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <ItemsControl ItemsSource="{Binding Items}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="{DynamicResource SurfaceBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                                                    CornerRadius="3" Padding="6,3" Margin="4,2"
                                                    ToolTip="{Binding DisabledReason}"
                                                    MouseMove="PaletteItem_MouseMove"
                                                    MouseLeftButtonDown="PaletteItem_MouseLeftButtonDown">
                                                <TextBlock Text="{Binding DisplayName}">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsAvailable}" Value="False">
                                                                    <Setter Property="Foreground" Value="{DynamicResource DisabledTextBrush}" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                </ScrollViewer>
            </DockPanel>

            <Border Grid.Column="1" x:Name="ViewportHost" Background="{DynamicResource CanvasBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1,0,1,0"
                    ClipToBounds="True" AllowDrop="True" Drop="Canvas_Drop" Focusable="True" FocusVisualStyle="{x:Null}"
                    PreviewMouseDown="ViewportHost_PreviewMouseDown"
                    MouseWheel="Viewport_MouseWheel" MouseDown="Viewport_MouseDown" MouseLeftButtonDown="Canvas_MarqueeStart">
                <Grid x:Name="CanvasRoot">
                    <Grid.RenderTransform>
                        <TransformGroup>
                            <ScaleTransform ScaleX="{Binding Viewport.Scale}" ScaleY="{Binding Viewport.Scale}" />
                            <TranslateTransform X="{Binding Viewport.OffsetX}" Y="{Binding Viewport.OffsetY}" />
                        </TransformGroup>
                    </Grid.RenderTransform>
                    <!-- Connection layer (under the nodes) -->
                    <ItemsControl ItemsSource="{Binding Connections}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate><Canvas /></ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Canvas>
                                    <Path Data="{Binding PathData}" Stroke="Transparent" StrokeThickness="10"
                                          MouseLeftButtonDown="Connection_MouseLeftButtonDown" />
                                    <Path Data="{Binding PathData}" StrokeThickness="2" IsHitTestVisible="False"
                                          Stroke="{Binding IsSelected, Converter={x:Static local:SelectionBorderConverter.Instance}}" />
                                </Canvas>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <!-- Node layer -->
                    <ItemsControl x:Name="NodeHost" ItemsSource="{Binding Nodes}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate><Canvas /></ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemContainerStyle>
                            <Style TargetType="ContentPresenter">
                                <Setter Property="Canvas.Left" Value="{Binding X}" />
                                <Setter Property="Canvas.Top" Value="{Binding Y}" />
                            </Style>
                        </ItemsControl.ItemContainerStyle>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Width="{x:Static core:NodeLayout.CardWidth}" Height="{Binding Height}" CornerRadius="6" Background="{DynamicResource SurfaceBackgroundBrush}"
                                        BorderThickness="2"
                                        MouseLeftButtonDown="Node_MouseLeftButtonDown">
                                    <Border.BorderBrush>
                                        <MultiBinding Converter="{x:Static local:NodeBorderConverter.Instance}">
                                            <Binding Path="IsSelected" />
                                            <Binding Path="RunState" />
                                        </MultiBinding>
                                    </Border.BorderBrush>
                                    <Grid>
                                        <StackPanel>
                                            <Border Background="{Binding CategoryColor, Converter={StaticResource CategoryBrush}}"
                                                    CornerRadius="6,6,0,0" Padding="6,3" MinHeight="28">
                                                <TextBlock Text="{Binding Label}" Foreground="White" FontWeight="Bold" />
                                            </Border>
                                            <TextBlock Text="{Binding TargetBadge}" FontSize="10" Foreground="{DynamicResource SecondaryTextBrush}" Margin="6,2"
                                                       Visibility="{Binding TargetBadge, Converter={x:Static local:NullToCollapsedConverter.Instance}}" />
                                        </StackPanel>
                                        <ItemsControl ItemsSource="{Binding InputPorts}">
                                            <ItemsControl.ItemsPanel><ItemsPanelTemplate><Canvas /></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemContainerStyle>
                                                <Style TargetType="ContentPresenter">
                                                    <Setter Property="Canvas.Left" Value="{Binding AnchorOffset.X}" />
                                                    <Setter Property="Canvas.Top" Value="{Binding AnchorOffset.Y}" />
                                                </Style>
                                            </ItemsControl.ItemContainerStyle>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Ellipse Width="10" Height="10" Fill="{DynamicResource SecondaryTextBrush}" ToolTip="{Binding Name}"
                                                             RenderTransform="{x:Static local:PortCenteringTransform.Instance}"
                                                             MouseLeftButtonDown="InputPort_MouseLeftButtonDown" />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                        <ItemsControl ItemsSource="{Binding OutputPorts}">
                                            <ItemsControl.ItemsPanel><ItemsPanelTemplate><Canvas /></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemContainerStyle>
                                                <Style TargetType="ContentPresenter">
                                                    <Setter Property="Canvas.Left" Value="{Binding AnchorOffset.X}" />
                                                    <Setter Property="Canvas.Top" Value="{Binding AnchorOffset.Y}" />
                                                </Style>
                                            </ItemsControl.ItemContainerStyle>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Ellipse Width="10" Height="10" Fill="{DynamicResource SecondaryTextBrush}" ToolTip="{Binding Name}"
                                                             RenderTransform="{x:Static local:PortCenteringTransform.Instance}"
                                                             MouseLeftButtonDown="OutputPort_MouseLeftButtonDown" />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <Canvas IsHitTestVisible="False">
                        <Rectangle x:Name="MarqueeRect" Visibility="Collapsed"
                                   Fill="#332962D6" Stroke="#2962D6" StrokeThickness="1" />
                    </Canvas>
                </Grid>
            </Border>

            <Border Grid.Column="2" Background="{DynamicResource PanelBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1,0,0,0">
                <Grid DataContext="{Binding Properties}">
                    <TextBlock Text="Select a node to edit its properties." Foreground="{DynamicResource SecondaryTextBrush}" Margin="8" TextWrapping="Wrap">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Node}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ScrollViewer.Style>
                            <Style TargetType="ScrollViewer">
                                <Setter Property="Visibility" Value="Visible" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Node}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ScrollViewer.Style>
                        <StackPanel Margin="8">
                            <TextBlock Text="{Binding ActionTitle}" FontWeight="Bold" FontSize="14" Margin="0,0,0,6" />

                            <TextBlock Text="Label" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" />
                            <TextBox Text="{Binding Node.Label, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

                            <TextBlock Text="Target" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" />
                            <ComboBox ItemsSource="{Binding Targets}" DisplayMemberPath="Name"
                                      SelectedValuePath="Id" SelectedValue="{Binding SelectedTargetId}"
                                      Margin="0,0,0,8" ToolTip="Unset = the first target" />

                            <ItemsControl ItemsSource="{Binding Fields}"
                                          ItemTemplateSelector="{StaticResource FieldSelector}" />

                            <Button Content="Pick coordinates…" Margin="0,8,0,0" Padding="6,3" HorizontalAlignment="Left"
                                    Click="PickCoordinates_Click"
                                    Visibility="{Binding SupportsCoordinatePicking, Converter={StaticResource BoolToVis}}" />

                            <Button Content="Pick region…" Margin="0,4,0,0" Padding="6,3" HorizontalAlignment="Left"
                                    Click="PickRegion_Click"
                                    Visibility="{Binding SupportsRegionPicking, Converter={StaticResource BoolToVis}}" />

                            <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0" Margin="0,10,0,0" Padding="0,8,0,0"
                                    Visibility="{Binding SupportsRetry, Converter={StaticResource BoolToVis}}">
                                <StackPanel>
                                    <TextBlock Text="Retry" FontWeight="Bold" Margin="0,0,0,4" />
                                    <TextBlock Text="Max Attempts" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" />
                                    <TextBox Text="{Binding Node.RetryMaxAttempts, UpdateSourceTrigger=PropertyChanged}" />
                                    <TextBlock Text="Delay (ms)" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,4,0,0" />
                                    <TextBox Text="{Binding Node.RetryDelayMs, UpdateSourceTrigger=PropertyChanged}" />
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
    </DockPanel>
</Window>
